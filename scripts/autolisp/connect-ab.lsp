;;; CONNECT-AB.LSP
;;; Mason Turgeon — skeleton for connecting point A to point B if B is present.

(defun c:CONNECT_AB ( / )
  (prompt "\nCONNECT_AB starting...")
  ;; TODO:
  ;; 1) Find blocks by name (e.g., A_BLOCK, B_BLOCK)
  ;; 2) Read endpoint attributes (e.g., A_POINT, B_POINT)
  ;; 3) If both present, draw a line or polyline between points
  (prompt "\n[stub] Implement search + connect logic here.")
  (princ)
)
